---
phase: 4
slug: lint-baseline
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-19
---

# Phase 4 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd-plan-phase 4`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd-execute-phase 4`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `package_linter` (`main`) — the linter *is* the test suite for this phase; no conventional unit-test framework |
| **Config file** | none — Wave 0 creates `scripts/run_lint.sh` wrapper |
| **Quick run command** | `wsl -e bash -lc 'bash <repo>/scripts/run_lint.sh'` |
| **Full suite command** | same (linter is the full suite; nothing else in Phase 4) |
| **Estimated runtime** | ~20–40 s (dominated by `YunoHost/apps` git clone on first run; cached ~1 h) |
| **CI pipeline** | none for Phase 4 — GH Actions workflow is Phase 7 (GHCI-01) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `wsl -e bash -lc 'bash <repo>/scripts/run_lint.sh'`
- **After every plan wave:** Run the same full-suite command (linter run + JSON assertion)
- **Before `/gsd-verify-work`:** JSON assertion must be green at the agreed scope + text artifact archived
- **Maximum acceptable task feedback latency:** ~40 s

---

## Per-task Verification Map

| task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 04-01-01 | 01 | 1 | LINT-01 | smoke | `wsl -e bash -lc 'bash <repo>/scripts/run_lint.sh && test -f .planning/.../lint-baseline.txt && test -f .planning/.../lint-baseline.json'` | ❌ W0 | ○ pending |
| 04-01-02 | 01 | 1 | LINT-01 | unit | `wsl -e bash -lc 'git ls-files --eol scripts/ \| grep -q "i/lf" && ! git ls-files --eol scripts/ \| grep -qE "i/crlf\|w/crlf"'` (fails on any CRLF entry) | ❌ W0 | ○ pending |
| 04-01-03 | 01 | 1 | LINT-01 | smoke | `wsl -e bash -lc 'test -f .planning/.../04-SCOPE-EXEMPTIONS.md && grep -q "AppCatalog.is_in_catalog" ...]'` | ❌ W0 | ○ pending |
| 04-02-01 | 02 | 2 | LINT-01 | unit | `wsl -e bash -lc '/tmp/pl/.venv/bin/python -c "import tomllib; ..."'` (manifest structure) | ❌ W0 | ○ pending |
| 04-02-02 | 02 | 2 | LINT-01 | integration | `wsl -e bash -lc 'bash <repo>/scripts/run_lint.sh'` (script deprecation fixes) | ❌ W0 | ○ pending |
| 04-03-01 | 03 | 3 | LINT-01 | integration | `wsl -e bash -lc 'bash <repo>/scripts/run_lint.sh'` (nginx/service modernization) | ❌ W0 | ○ pending |
| 04-04-01 | 04 | 4 | LINT-01 | integration | `wsl -e bash -lc '/tmp/pl/.venv/bin/python - <<PY ... json.load(lint-after-fix.json); assert no non-exempt critical/error ... PY'` | ❌ W0 | ○ pending |
| 04-04-02 | 04 | 4 | LINT-01 | smoke | `wsl -e bash -lc '/tmp/pl/.venv/bin/python - <<PY ... json.load(lint-after-fix.json); assert ... PY'` (phase gate, identical to 04-04-01) | ❌ W0 | ○ pending |

*All JSON assertions use the Wave 0 venv interpreter `/tmp/pl/.venv/bin/python` (Python 3.12). `jq` is NOT installed in WSL; system `python3` is 3.8.10 (no `tomllib`).*
*Status values: ○ pending → ✓ green → ✗ red → ⚡ flaky*

---

## Wave 0 Requirements

> Scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [x] `scripts/run_lint.sh` — deterministic wrapper: ensures `uv`/venv, runs linter with `/mnt/c/...` POSIX path, writes both text + `--json` artifacts. **Single most important Wave 0 deliverable.**
- [x] `.gitattributes` — pins LF for `scripts/*` and text files (prevents CRLF corruption)
- [x] `.planning/phases/04-lint-baseline/lint-baseline.txt` — archived text artifact (before fixes)
- [x] `.planning/phases/04-lint-baseline/lint-baseline.json` — archived JSON artifact (before fixes)
- [x] Framework install: WSL2 + `uv` + Python 3.12 + linter clone under `/tmp/pl` (one-time; never committed)
- [x] Decision record for catalog-dependent findings (out-of-scope-by-design documentation)

*The phase has no conventional unit-test framework. The linter IS the test. Tests = deterministic run + artifact + assertion.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| nginx `Upgrade`/`Connection` header removal preserves LibreChat WebSockets | LINT-01 | Requires a running install to exercise WS traffic; Phase 4 has no Linux app host | Verify `include proxy_params_no_auth` supplies the needed headers before removal; if uncertain, keep the headers and document. Full WS check deferred to Phase 6 live run. |

*All other phase behaviors have automated verification coverage.*

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 60s ✓
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved (revision 2026-09-19 — jq assertions replaced with Wave 0 venv Python; 04-02 interpreter fixed to `/tmp/pl/.venv/bin/python`)

---

## Execution Tracking

Updated during `/gsd-execute-phase 4`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 4 | `run_lint.sh` | — | — | scaffold |
| 1 | 2 | `run_lint.sh` | — | — | ○ pending |
| 2 | 2 | `run_lint.sh` | — | — | ○ pending |
| 3 | 2 | `run_lint.sh` | — | — | ○ pending |
| 4 | 2 | `run_lint.sh` + venv-Python assertion | — | — | ○ pending |

**Phase validation complete:** pending (plans approved 2026-09-19; execution not yet started)
