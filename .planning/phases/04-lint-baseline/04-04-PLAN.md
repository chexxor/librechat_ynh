---
phase: 04-lint-baseline
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .planning/phases/04-lint-baseline/lint-before-fix.txt
  - .planning/phases/04-lint-baseline/lint-before-fix.json
  - .planning/phases/04-lint-baseline/lint-after-fix.txt
  - .planning/phases/04-lint-baseline/lint-after-fix.json
  - .planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md
  - .planning/phases/04-lint-baseline/04-04-SUMMARY.md
autonomous: false
requirements: [LINT-01]

must_haves:
  truths:
    - "A full linter run after all fixes shows zero criticals and zero errors in the locally-fixable set"
    - "The baseline artifacts show the before-fix list AND the after-fix list so the delta is auditable"
    - "Every exempted finding in the final JSON appears in 04-SCOPE-EXEMPTIONS.md with a reason"
    - "The user has explicitly confirmed the scope-adjusted zero-error interpretation"
  artifacts:
    - path: ".planning/phases/04-lint-baseline/lint-before-fix.txt"
      provides: "Archived pre-fix text artifact"
    - path: ".planning/phases/04-lint-baseline/lint-before-fix.json"
      provides: "Archived pre-fix JSON artifact"
    - path: ".planning/phases/04-lint-baseline/lint-after-fix.txt"
      provides: "Archived post-fix text artifact"
    - path: ".planning/phases/04-lint-baseline/lint-after-fix.json"
      provides: "Final post-fix finding set (assertion target)"
    - path: ".planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md"
      provides: "Final exemption accounting reconciled against the post-fix JSON"
    - path: ".planning/phases/04-lint-baseline/04-04-SUMMARY.md"
      provides: "Phase-level lint-baseline result record"
  key_links:
    - from: ".planning/phases/04-lint-baseline/lint-after-fix.json"
      to: "04-SCOPE-EXEMPTIONS.md"
      via: "every remaining critical/error is listed as exempt and every exempt entry is justified"
      pattern: "AppCatalog\\.is_in_catalog"
    - from: "scripts/run_lint.sh"
      to: ".planning/phases/04-lint-baseline/lint-after-fix.json"
      via: "final deterministic run"
      pattern: "run_lint.sh"
---

<objective>
Run the linter one final time after all fixes, archive the post-fix artifacts, reconcile the scope-exemption record against the actual remaining findings, and obtain the user's explicit confirmation of the "zero locally-fixable errors" interpretation.

Purpose: This closes LINT-01 with an auditable, honest zero-finding claim. The invariant is `(critical ∪ error) \ exempted == ∅`. Because the interpretation is a judgment call (locked decision 1), the final task is a user checkpoint.
Output: post-fix artifacts, reconciled exemption record, phase SUMMARY, user sign-off.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-lint-baseline/04-CONTEXT.md
@.planning/phases/04-lint-baseline/04-RESEARCH.md
@.planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md
@.planning/phases/04-lint-baseline/lint-baseline.json

> The referenced `lint-baseline.*` files are the Plan 01 scratch artifacts; task 1 renames them to `lint-before-fix.*` and produces the `lint-after-fix.*` pair.

**Runner (owned by Plan 01 — this plan only executes it):** `wsl -e bash -lc 'bash /mnt/c/Users/Alex/Projects/librechat_ynh/scripts/run_lint.sh'`.
**JSON assertions use the Wave 0 venv interpreter** `/tmp/pl/.venv/bin/python` (Python 3.12). `jq` is NOT installed in WSL and system `python3` is 3.8.10 (no `tomllib`) — never use either for JSON assertions.
**Invariant:** `critical ∪ error` minus the exempted set (`AppCatalog.is_in_catalog`, `AppCatalog.state_is_working`, `Configurations.tests_toml`) must be empty. `AppCatalog.has_category` is a warning and also exempted.
**Critical rule (Pitfall 5):** `--json` ALWAYS exits 0. Never assert on exit code; assert on the JSON body.
**Do not modify `scripts/run_lint.sh` or any package file** — this plan is verification + documentation only. If the final run reveals a NEW fixable finding, do NOT fix it here; return it to the orchestrator as a gap.
</context>

<tasks>

<task type="auto">
  <name>task 1: Final run, archive post-fix artifacts, reconcile exemptions</name>
  <files>.planning/phases/04-lint-baseline/lint-before-fix.txt, .planning/phases/04-lint-baseline/lint-before-fix.json, .planning/phases/04-lint-baseline/lint-after-fix.txt, .planning/phases/04-lint-baseline/lint-after-fix.json, .planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md</files>
  <action>
1. Run the linter once more: `wsl -e bash -lc 'bash /mnt/c/Users/Alex/Projects/librechat_ynh/scripts/run_lint.sh'`. This overwrites `lint-baseline.json`/`lint-baseline.txt` in place — preserve the before-fix copies first:
   - Before running, copy the current (before-fix) artifacts to `lint-before-fix.txt` and `lint-before-fix.json` in the phase dir. (They were captured in Plan 01; renaming preserves the before/after delta.)
   - Optionally remove the now-superseded `lint-baseline.txt`/`lint-baseline.json` names after the rename, OR leave them as the runner's scratch output — the canonical deliverables are the `lint-before-fix.*` / `lint-after-fix.*` pair. Document the final layout in the SUMMARY.

2. After the run, copy the fresh artifacts to `lint-after-fix.txt` and `lint-after-fix.json` (the run overwrote the originals; the after-fix copies are the deliverable record). Print the bucket counts using the Wave 0 venv interpreter (Python 3.12) — **do NOT use `jq`; it is not installed in WSL** and is not provisioned by the runner. Run:

```
wsl -e bash -lc 'cd /mnt/c/Users/Alex/Projects/librechat_ynh/.planning/phases/04-lint-baseline && \
  /tmp/pl/.venv/bin/python - <<"PY"
import json
d = json.load(open("lint-after-fix.json"))
print({"critical": d["critical"], "error": d["error"], "warning": len(d["warning"]), "info": len(d["info"])})
PY'
```

3. Compute the invariant. The exempted findings are exactly:
   - critical: `AppCatalog.is_in_catalog`
   - error: `AppCatalog.state_is_working`, `Configurations.tests_toml`
   - warning: `AppCatalog.has_category` (and possibly the retained nginx Upgrade/Connection warning)
   
   Assert: after removing the exempted entries, both `critical` and `error` arrays are empty. Use the venv interpreter (not jq):

```
wsl -e bash -lc 'cd /mnt/c/Users/Alex/Projects/librechat_ynh/.planning/phases/04-lint-baseline && \
  /tmp/pl/.venv/bin/python - <<"PY"
import json, sys
d = json.load(open("lint-after-fix.json"))
left_crit = [x for x in d["critical"] if x != "AppCatalog.is_in_catalog"]
left_err  = [x for x in d["error"] if x not in ("AppCatalog.state_is_working", "Configurations.tests_toml")]
assert left_crit == [], left_crit
assert left_err == [], left_err
print("LOCALLY-FIXABLE-ERRORS-ZERO")
PY'
```
   If this FAILS, do NOT fix anything in this plan — record the unexpected findings in the SUMMARY and return to the orchestrator (they are gap-closure work).

4. Reconcile `04-SCOPE-EXEMPTIONS.md` against the actual post-fix JSON:
   - Update the baseline totals section with the ACTUAL after-fix numbers.
   - If the final JSON contains any critical/error NOT in the exempted list, that is a failure — list it explicitly.
   - If the retained nginx warning appears (or does not), update the table's nginx row to match reality.
   - Confirm every exempted heading in the JSON is present in the table and vice versa.

5. Confirm the artifact layout: the canonical record is the `lint-before-fix.*` / `lint-after-fix.*` pair (both text and JSON for each). The runner's scratch `lint-baseline.txt`/`lint-baseline.json` may remain or be removed; document whichever layout you settle on in the SUMMARY.

Avoid: asserting on the linter's exit code; using `jq` (not installed in WSL); editing any file outside `.planning/phases/04-lint-baseline/`; re-running with a Windows path.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /mnt/c/Users/Alex/Projects/librechat_ynh/.planning/phases/04-lint-baseline && test -f lint-before-fix.json && test -f lint-after-fix.json && test -f lint-after-fix.txt && /tmp/pl/.venv/bin/python - <<"PY"
import json
d = json.load(open("lint-after-fix.json"))
assert [x for x in d["critical"] if x != "AppCatalog.is_in_catalog"] == []
assert [x for x in d["error"] if x not in ("AppCatalog.state_is_working", "Configurations.tests_toml")] == []
print("ASSERTION-PASS")
PY'</automated>
    <manual>Post-fix JSON shows only the documented exempted criticals/errors remain; exemption record matches the JSON exactly. NOTE: assertions use the Wave 0 venv interpreter `/tmp/pl/.venv/bin/python` (Python 3.12); `jq` is NOT installed in WSL, and system `python3` is 3.8.10 without `tomllib`.</manual>
    <sampling_rate>final phase gate — run after all fix tasks are committed</sampling_rate>
  </verify>
  <done>Post-fix artifacts archived (before+after); the scope-adjusted invariant assertion passes; 04-SCOPE-EXEMPTIONS.md reconciled against the actual final JSON with no unexplained findings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: User confirms the scope-adjusted zero-error interpretation</name>
  <files>.planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md, .planning/phases/04-lint-baseline/lint-after-fix.json</files>
  <what-built>
The complete Phase 4 lint baseline: a deterministic WSL2 runner, LF pinning, all locally-fixable findings fixed (2 manifest schema violations + 8 warnings), the nginx 4-header removal with WS headers deliberately retained, a regenerated README, and a reconciled scope-exemption record. The final linter run has locally-fixable criticals/errors at zero.
  </what-built>
  <how-to-verify>
1. Review the final finding set: `wsl -e bash -lc 'cd /mnt/c/Users/Alex/Projects/librechat_ynh/.planning/phases/04-lint-baseline && /tmp/pl/.venv/bin/python -m json.tool lint-after-fix.json'` — confirm the only remaining `critical`/`error` entries are the three documented catalog/tests.toml exemptions.
2. Read `.planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md` — confirm you agree with each exemption reason (catalog PR out of scope; tests.toml is Phase 5; nginx WS headers deliberate).
3. Diff the delta: compare `lint-before-fix.json` vs `lint-after-fix.json` — every non-exempted before-fix finding should be gone.
4. Sanity-check the nginx change preserved WS support: `conf/nginx.conf` still has `proxy_http_version 1.1`, `Upgrade`, `Connection` and the `include proxy_params_no_auth` line.
5. Confirm the repo is clean of the linter clone: `git status` shows no `/tmp/pl` or venv additions.
  </how-to-verify>
  <action>
    The executor presents the post-fix JSON bucket counts, the exemption table, and the before/after delta to the user, then pauses. The user reviews and either approves the scope-adjusted zero-error claim or points out a disagreement. The executor does not proceed until a resume signal is received. This task has no automation to perform — all automated verification happened in task 1.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /mnt/c/Users/Alex/Projects/librechat_ynh/.planning/phases/04-lint-baseline && /tmp/pl/.venv/bin/python - <<"PY"
import json
d = json.load(open("lint-after-fix.json"))
assert [x for x in d["critical"] if x != "AppCatalog.is_in_catalog"] == []
assert [x for x in d["error"] if x not in ("AppCatalog.state_is_working", "Configurations.tests_toml")] == []
print("PHASE-GATE-PASS")
PY'</automated>
    <manual>User reads 04-SCOPE-EXEMPTIONS.md and lint-after-fix.json and confirms the scope-adjusted interpretation is acceptable</manual>
    <sampling_rate>phase-complete gate</sampling_rate>
  </verify>
  <done>User has typed "approved" (or recorded a specific disagreement to return to the orchestrator as a gap).</done>
  <resume-signal>Type "approved" to accept the scope-adjusted zero-error claim, or describe which exemption/fix you disagree with</resume-signal>
</task>

</tasks>

<verification>
- `lint-before-fix.*` and `lint-after-fix.*` (text + JSON each) both exist in the phase dir
- The venv-Python invariant assertion passes (locally-fixable criticals and errors are empty)
- `04-SCOPE-EXEMPTIONS.md` matches the final JSON exactly (no orphan exemptions, no unexplained findings)
- User types "approved"
- `git status` shows the repo is clean (no linter clone/venv)
</verification>

<success_criteria>
LINT-01 is satisfied as: `package_linter` reports zero locally-fixable errors, all 9 warnings either fixed (8) or documented as out-of-scope-by-design (1 — catalog category), and a before/after baseline is archived in the phase directory. The user has explicitly confirmed this interpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lint-baseline/04-04-SUMMARY.md` (include the final JSON bucket counts, the exempted-finding list, and the artifact layout).
</output>
