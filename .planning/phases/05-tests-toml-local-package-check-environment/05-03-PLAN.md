---
phase: 05-tests-toml-local-package-check-environment
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/phases/05-tests-toml-local-package-check-environment/Test_results.log
  - .planning/phases/05-tests-toml-local-package-check-environment/tests.toml
  - .planning/phases/05-tests-toml-local-package-check-environment/package_check-full.log
  - .planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md
autonomous: false
requirements:
  - CI-02

must_haves:
  truths:
    - "The user has provisioned the Hyper-V Debian 12 VM and it is SSH-reachable from Windows with the existing id_rsa key"
    - "The full package_check suite starts and completes (a Global level summary is printed) with no Critical: abort, crash, or timeout"
    - "The Phase 5 findings run is archived in the phase dir (Test_results.log + a copy of tests.toml + a findings note)"
    - "A human confirms the documented steps were followed with no undocumented steps"
  artifacts:
    - path: ".planning/phases/05-tests-toml-local-package-check-environment/Test_results.log"
      provides: "Archived package_check findings run (Phase 5 evidence; NOT the Phase 6 zero-failure archive)"
      min_lines: 5
    - path: ".planning/phases/05-tests-toml-local-package-check-environment/tests.toml"
      provides: "Exact copy of the tests.toml used for the run"
      contains: "test_format = 1.0"
    - path: ".planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md"
      provides: "Human-readable Phase 5 findings summary and pointer to doc/PACKAGE_CHECK.md and the Phase 6 handoff"
      min_lines: 20
  key_links:
    - from: "05-03 run"
      to: "tests.toml"
      via: "package_check consumes the repo-root tests.toml created in 05-01"
      pattern: "test_format"
    - from: "05-03 run"
      to: "scripts/setup_pc_env.sh"
      via: "the run happens on a host provisioned by the 05-02 setup path"
      pattern: "setup_pc_env"

user_setup:
  - service: hyperv-vm
    why: "One-time user provisioning of the Debian 12 package_check host — OpenCode cannot create a VM or use cloud credentials"
    env_vars: []
    dashboard_config:
      - task: "Create Hyper-V Gen 2 VM: Debian 12 netinst ISO, second vdisk for btrfs, OpenSSH server, host-only/internal switch + static IP; follow doc/PACKAGE_CHECK.md"
        location: "Windows Hyper-V Manager -> New -> Virtual Machine"
      - task: "Confirm SSH from Windows: ssh <user>@<static-ip> using the existing id_rsa key"
        location: "Windows PowerShell"
---

<objective>
Run the full `package_check` suite once on the user-provisioned Hyper-V Debian 12 VM and archive the findings run. Phase 5's bar is that the suite **starts and completes** — findings are allowed and expected; crashes and timeouts are not. Zero failures is Phase 6 (POLS-01).

Purpose: This is the payoff of the phase — proof that the environment and tests.toml actually drive a complete end-to-end package_check run, and the source of the real finding list for Phase 6 to fix. The VM is a one-time user step, so this plan is `autonomous: false` with checkpoints around provisioning and the final verification.

Output: `Test_results.log`, `tests.toml` copy, `package_check-full.log`, and `05-FINDINGS.md` archived in the phase directory.
</objective>

<execution_context>
@C:\Users\Alex\.config\opencode\get-shit-done\workflows\execute-plan.md
@C:\Users\Alex\.config\opencode\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-CONTEXT.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-RESEARCH.md
@doc/PACKAGE_CHECK.md
@scripts/setup_pc_env.sh
@tests.toml
@.planning/phases/05-tests-toml-local-package-check-environment/05-01-SUMMARY.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-02-SUMMARY.md

**User decisions (locked — do not revisit):**
- VM provisioning is a one-time USER step (checkpoint below). OpenCode drives everything after over SSH from Windows using the existing `id_rsa` key and a host-only static IP.
- Phase 5 bar: full suite starts and completes; findings allowed, crashes/timeouts not. Zero-failures is Phase 6.
- Archive the Phase 5 findings run in the phase dir; keep it distinct from Phase 6's POLS-01 zero-failure archive.
- Do NOT add `exclude`, do NOT add `only` to `[default]`, do NOT add a private-install suite.

**RESEARCH CORRECTIONS (higher-confidence than CONTEXT — apply these):**
- The deduced suite is `package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url`. `install.subdir` is suppressed (no `[install.path]` question) and `install.private` is NEVER auto-generated — do NOT expect them. `change_url` will fail (no script) — expected Phase 6 finding.
- `Test_results.log`, `full_log_0.log`, `results_0.json` are written in the **package_check clone root**, not the app dir.
- package_check has **no per-test timeout**; long installs are bounded only by YunoHost limits and the SSH session. Run under `tmux`/`nohup` or with `ssh -o ServerAliveInterval=30`, tee to a log, and poll.
- Expected findings from the research: `change_url` fail, `package_linter` fail (Phase 4 catalog criticals/errors), possibly long-install flakiness.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User provisions the Hyper-V Debian 12 VM (one-time)</name>
  <files></files>
  <action>
This is the one unavoidable manual step: OpenCode cannot create a Hyper-V VM or use cloud credentials. The user provisions the VM by following the "One-time VM provisioning" section of `doc/PACKAGE_CHECK.md`.

Provide the user with the exact checklist (from doc/PACKAGE_CHECK.md):
1. Hyper-V Manager -> New -> Virtual Machine, **Generation 2**.
2. Attach the **Debian 12 netinst ISO**.
3. During Debian install, enable the **OpenSSH server** task.
4. Add a **second virtual disk** dedicated to the btrfs pool (e.g. 40–60 GB; do not put the OS on it).
5. Network: **host-only/internal switch** with a **static IP** (no conflicting DNS on :53; the VM must own the bridge and dnsmasq:53).
6. Sizing guidance: 8 GB RAM, 4 vCPU, 60 GB OS disk (record actual values used).
7. From Windows, verify SSH with the existing key: `ssh -i $env:USERPROFILE\.ssh\id_rsa <user>@<static-ip>`.

Record the values the user actually used (IP, user, RAM/vCPU/disk) — they are needed for the SSH-driven tasks and for the reproducibility claim. If the user prefers Debian 13 (native incus, no Zabbly repo), that is a documented alternative but it contradicts the locked Debian 12 decision — surface it rather than changing silently.

Do NOT proceed to Task 2 until the VM is SSH-reachable.
  </action>
  <verify>
    <automated>ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes -o ConnectTimeout=10 "$PC_VM" "uname -a && . /etc/os-release && echo $VERSION_CODENAME" | grep -qi 'bookworm\|debian' && echo "VM reachable over SSH"</automated>
    <manual>User confirms the VM was created per doc/PACKAGE_CHECK.md and SSH succeeds from Windows.</manual>
    <sampling_rate>gate before Task 2 — do not run any suite until this passes</sampling_rate>
  </verify>
  <resume-signal>Type "vm-ready" with the SSH target (user@static-ip) and sizing used, or describe blockers.</resume-signal>
  <done>
A Hyper-V Generation 2 Debian 12 VM exists with a second disk for btrfs, OpenSSH enabled, a host-only static IP, and verified SSH access from the Windows dev box; actual sizing recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Provision the in-VM environment and copy the package over SSH</name>
  <files></files>
  <action>
From Windows, over SSH, using the 05-02 setup path:

1. Copy the current package tree to the VM (package_check accepts a git URL, but a local path matches the "local dev" intent and avoids pushing WIP commits): `ssh $PC_VM "rm -rf ~/librechat_ynh && mkdir -p ~/librechat_ynh"` then `scp -r -i $env:USERPROFILE\.ssh\id_rsa .\* "${PC_VM}:~/librechat_ynh/"`. Exclude `.git` scratch if desired; the `test_upgrade_from.05e3d5b` checkout needs the `.git` history, so **include `.git`** (or clone the repo URL on the VM instead — pick one and document it).
2. Run the host setup: `ssh $PC_VM "sudo bash ~/librechat_ynh/scripts/setup_pc_env.sh <user>"`. Run `incus admin init` per `doc/PACKAGE_CHECK.md` (interactive, or `--minimal` then let the script create/repoint the btrfs pool).
3. Re-establish group membership after `usermod -aG incus-admin`: `ssh $PC_VM "newgrp incus-admin"` or re-SSH.
4. Sanity checks and capture their output into `.planning/phases/05-tests-toml-local-package-check-environment/env-sanity.txt`:
   - `incus storage list` shows driver **btrfs** (NOT `dir`),
   - `ip a | grep incusbr0` present,
   - `incus remote list` includes `yunohost`,
   - `command -v lynx jq btrfs-progs python3` all resolve,
   - `ls ~/package_check/package_check.sh` exists.
5. Confirm `~/librechat_ynh/tests.toml` exists on the VM and matches the repo copy (`diff` against the local file).

Avoid: running the suite before the storage pool is confirmed btrfs; setting up on the OS disk; skipping the group re-login (Incus commands will permission-fail).
  </action>
  <verify>
    <automated>
# Storage driver must be btrfs and the bridge/remote/deps must be present.
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'incus storage list --format csv | cut -d, -f1,2 | grep -q btrfs_pool,btrfs' && echo "btrfs pool OK"
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'ip a | grep -q incusbr0' && echo "incusbr0 OK"
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'incus remote list -f json | jq -e .yunohost >/dev/null' && echo "yunohost remote OK"
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'command -v lynx jq btrfs-progs python3' >/dev/null && echo "deps OK"
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'test -x ~/package_check/package_check.sh' && echo "package_check OK"
ssh -i "$HOME/.ssh/id_rsa" -o BatchMode=yes "$PC_VM" 'test -f ~/librechat_ynh/tests.toml && grep -q "test_format = 1.0" ~/librechat_ynh/tests.toml' && echo "tests.toml on VM OK"
    </automated>
    <manual>Record the sanity output; confirm the pool driver is btrfs (a `dir` pool means snapshots are slow and the setup step was skipped).</manual>
    <sampling_rate>gate before Task 3 — the suite will be unreliable on a dir-backed pool</sampling_rate>
  </verify>
  <done>
The VM has the package_check dependencies, Incus on a btrfs pool, the `incusbr0` bridge, the `yunohost` remote, a package_check clone, and the package tree (with the new tests.toml) present; sanity output captured.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the full package_check suite once and retrieve the logs</name>
  <files>
.planning/phases/05-tests-toml-local-package-check-environment/Test_results.log
.planning/phases/05-tests-toml-local-package-check-environment/package_check-full.log
.planning/phases/05-tests-toml-local-package-check-environment/tests.toml
  </files>
  <action>
Start the full suite ONCE on the VM, decoupled from the SSH session so a disconnect cannot kill it, then poll and retrieve.

1. Optional pre-flight (cheap): `./package_check.sh -D ~/librechat_ynh` to confirm the deduced suite matches `package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url` before paying the full-run cost.
2. Launch under tmux/nohup with a keepalive:
   `ssh -o ServerAliveInterval=30 $PC_VM "cd ~/package_check && tmux new-session -d -s pc './package_check.sh ~/librechat_ynh 2>&1 | tee ~/pc_run.log'"`.
   If a stale lock exists (`pcheck-0.lock` with a live PID), clean it with `./package_check.sh -s` before launching.
3. Poll progress (do not block a single SSH call for hours): periodically `ssh $PC_VM "tail -n 30 ~/pc_run.log"` and `ssh $PC_VM "ls -l ~/package_check/Test_results.log"`. The run likely takes 1–3 hours (each install does MongoDB apt + `npm ci` + `turbo build`).
4. Completion detection: a **Global level summary** is printed at the end of `Test_results.log`, and there is no `Critical:` abort. If the SSH session drops, reconnect and re-check — the tmux session keeps running.
5. Retrieve artifacts into the phase dir:
   - `scp "${PC_VM}:~/package_check/Test_results.log" ".planning/phases/05-tests-toml-local-package-check-environment/Test_results.log"`
   - `scp "${PC_VM}:~/pc_run.log" ".planning/phases/05-tests-toml-local-package-check-environment/package_check-full.log"`
   - `cp tests.toml ".planning/phases/05-tests-toml-local-package-check-environment/tests.toml"` (the exact file used).

Interpret results honestly: `change_url` failing (no `scripts/change_url`) and `package_linter` failing (Phase 4 catalog criticals) are **expected Phase 6 findings**, not Phase 5 crashes. A crash/timeout (no Global summary, run dies mid-install) IS a Phase 5 failure to be diagnosed and documented (e.g. VM sizing, SSH drop, timeout) — raise the VM resources or add `ssh -o ServerAliveInterval` and re-run; if it proves a package bug, record it for Phase 6 (do not add `exclude`).

Avoid: running the command directly in a foreground SSH session (a drop kills the run); adding `exclude`/`only` to make it green; starting a second concurrent run while one is active.
  </action>
  <verify>
    <automated>
PHASE=".planning/phases/05-tests-toml-local-package-check-environment"
test -f "$PHASE/Test_results.log"
test -f "$PHASE/package_check-full.log"
test -f "$PHASE/tests.toml"
grep -q 'test_format = 1.0' "$PHASE/tests.toml"
# Completed run: a Global level summary is present and there is no Critical: abort.
grep -qiE 'Global(\s+level)?|Global' "$PHASE/Test_results.log" || \
  { echo "No Global summary found — run may not have completed"; exit 1; }
! grep -qiE '^Critical:|\bCritical:' "$PHASE/Test_results.log"
# Sanity: the run actually started tests (test names appear).
grep -qiE 'install|upgrade|backup' "$PHASE/Test_results.log"
echo "Phase 5 full-suite run archived and completed"
    </automated>
    <manual>Read Test_results.log end-to-end; confirm every deduced test type ran to a result (pass or fail) and none crashed/timed out.</manual>
    <sampling_rate>run once at the end of the phase — this is the CI-02 acceptance artifact</sampling_rate>
  </verify>
  <done>
The full suite completed on the VM; `Test_results.log` (with a Global summary, no `Critical:` abort) plus `package_check-full.log` and the exact `tests.toml` are archived in the phase dir; expected findings (`change_url`, linter) are distinguishable from crashes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write 05-FINDINGS.md and hand off to Phase 6</name>
  <files>.planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md</files>
  <action>
Write `05-FINDINGS.md` summarizing the Phase 5 run for the Phase 6 consumer. Include:

1. **Run metadata:** date, VM sizing used (RAM/vCPU/disks), Incus channel (`lts-7.0`), Debian codename, package_check ref, total runtime, whether it completed.
2. **Deduced suite actually run:** `package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url` — note explicitly that `install.subdir` (no `[install.path]` question) and `install.private` (never auto-generated) did NOT run, and `install.multi` skipped (`multi_instance = false`). This corrects the CONTEXT/ROADMAP assumption and tells Phase 6 it may need an explicit private-install suite if POLS-01 requires one.
3. **Per-test results:** a table of test -> pass/fail -> one-line reason. Mark expected findings:
   - `change_url` fail (no `scripts/change_url`; POLS-02 deferred),
   - `package_linter` fail (Phase 4 catalog criticals/errors, scope-adjusted — see 04-SCOPE-EXEMPTIONS.md).
4. **Crash/timeout check:** state explicitly that no test crashed or timed out (or, if any did, document the cause and whether it is environmental vs a package bug, and what was changed to make the suite complete).
5. **Artifacts:** pointers to `Test_results.log`, `package_check-full.log`, `tests.toml`, `tests-deduction.json`, `tests-dryrun.txt`, `env-sanity.txt`, and `doc/PACKAGE_CHECK.md`.
6. **Phase 6 handoff:** list the findings to fix to reach zero failures (POLS-01), and note that Phase 6 archives its own zero-failure run — Phase 5 and Phase 6 evidence stay distinct.

Do not overstate: Phase 5's success is "starts and completes", not zero failures.
  </action>
  <verify>
    <automated>
F=".planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md"
test -f "$F"
grep -qi 'starts and completes\|completed' "$F"
grep -qi 'change_url' "$F"
grep -qi 'package_linter' "$F"
grep -qi 'Phase 6' "$F"
grep -qi 'install.private' "$F"
test "$(wc -l < "$F")" -ge 20
echo "findings handoff written"
    </automated>
    <manual>Confirm expected findings are labeled as such and the crash/timeout statement is honest.</manual>
    <sampling_rate>run after this task commits — closes the phase evidence package</sampling_rate>
  </verify>
  <done>
`05-FINDINGS.md` exists (≥20 lines) with run metadata, the actual deduced suite, per-test results labeled expected vs unexpected, an explicit no-crash/no-timeout statement, artifact pointers, and a Phase 6 handoff list.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User verifies reproducibility and accepts the Phase 5 findings run</name>
  <files></files>
  <action>
Present the user with the archived Phase 5 findings run and the reproducibility question. Everything automated is already done (Tasks 1-4); this checkpoint only asks the user to confirm the documented steps reproduce the environment and to accept the findings run. Use the <what-built> / <how-to-verify> content below as the prompt.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — no automated command; human confirmation is the gate"</automated>
    <manual>User confirms: suite completed (findings allowed), doc reproduces the environment with no undocumented steps, wording amendment correct, Phase 5 evidence distinct from Phase 6.</manual>
    <sampling_rate>final gate of the phase</sampling_rate>
  </verify>
  <what-built>
The full package_check suite was run once on the Hyper-V Debian 12 VM and its findings run archived in the phase dir (`Test_results.log`, `tests.toml`, `package_check-full.log`, `05-FINDINGS.md`). The environment was provisioned by following `doc/PACKAGE_CHECK.md` + `scripts/setup_pc_env.sh`, and the roadmap/requirements WSL2 wording was amended to the real host.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/05-tests-toml-local-package-check-environment/Test_results.log` and confirm the suite reached its Global summary with every deduced test type resolved (pass or fail) and no crash/timeout.
2. Confirm `05-FINDINGS.md` accurately lists the expected findings (`change_url`, `package_linter`) and does not overstate Phase 5 as "zero failures".
3. Reproducibility check: from `doc/PACKAGE_CHECK.md`, confirm no step you performed is missing from the doc (i.e. the doc alone would rebuild this environment from a fresh VM). Report any undocumented step.
4. Confirm `.planning/REQUIREMENTS.md` CI-02 and `.planning/ROADMAP.md` Phase 5 no longer say WSL2 and describe the Hyper-V Debian 12 + Incus + btrfs host.
5. Confirm the Phase 5 evidence is clearly distinct from the future Phase 6 zero-failure (POLS-01) archive.
  </how-to-verify>
  <resume-signal>Type "approved" if the run completed and the doc reproduces it; otherwise describe the gap (undocumented step, missing artifact, or a crash/timeout needing a re-run).</resume-signal>
  <done>
User confirms: suite completed (findings allowed), doc reproduces the environment with no undocumented steps, wording amendment is correct, and Phase 5 evidence is archived distinctly from Phase 6's future run.
  </done>
</task>

</tasks>

<verification>
- User-provisioned Hyper-V Debian 12 VM is SSH-reachable from Windows with the existing id_rsa key.
- VM environment sanity: btrfs pool driver (not dir), `incusbr0` present, `yunohost` remote, deps installed, package_check cloned.
- Full suite ran once under tmux/nohup; `Test_results.log` shows a Global summary with no `Critical:` abort and no crash/timeout.
- Phase dir contains `Test_results.log`, `tests.toml`, `package_check-full.log`, `05-FINDINGS.md` (+ `tests-deduction.json`, `tests-dryrun.txt`, `env-sanity.txt` from earlier tasks).
- Findings are labeled expected vs unexpected; Phase 5 vs Phase 6 evidence is distinct.
- No `exclude`/`only`/private-suite content was added anywhere.
</verification>

<success_criteria>
CI-02 satisfied at the Phase 5 bar: following the documented setup, the user (with OpenCode driving over SSH) runs the full `package_check` suite against the package, and it starts and completes with findings allowed — no crashes or timeouts. The findings run is archived in the phase dir and handed off to Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests-toml-local-package-check-environment/05-03-SUMMARY.md`
</output>
