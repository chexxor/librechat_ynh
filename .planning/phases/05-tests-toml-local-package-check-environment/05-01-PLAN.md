---
phase: 05-tests-toml-local-package-check-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests.toml
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "package_check's parser reads tests.toml and deduces a valid test suite without error"
    - "The deduced suite supplies admin_email so no install test aborts with 'Missing install arg admin_email ?'"
    - "The deduced suite contains upgrade.05e3d5b (the installable v1.0 ancestor), not upgrade.116691c"
    - "The deduced suite contains package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url — and no exclude block suppresses any of them"
  artifacts:
    - path: "tests.toml"
      provides: "package_check v1 test configuration (install args, upgrade-from, curl smoke-tests)"
      contains: "test_format = 1.0"
      min_lines: 20
  key_links:
    - from: "tests.toml"
      to: "manifest.toml"
      via: "args.admin_email supplies the no-default [install.admin_email] arg"
      pattern: "args\\.admin_email"
    - from: "tests.toml"
      to: "commit 05e3d5b"
      via: "test_upgrade_from.05e3d5b is the git ref package_check checks out"
      pattern: "test_upgrade_from\\.05e3d5b"

user_setup: []
---

<objective>
Create the schema-valid `tests.toml` at the package repo root so `package_check` can install LibreChat, run its curl smoke-tests, and exercise the upgrade-from-previous-version path.

Purpose: This is the CI-01 deliverable — without `tests.toml`, package_check cannot supply `admin_email`, the linter's `Configurations.tests_toml` error persists, and no suite can run. It also unblocks Phase 5's full-suite run (05-03) and Phase 6's zero-failures work. (Side effect: on a future lint run this clears the Phase 4-exempted `Configurations.tests_toml` error — a side effect, not the goal.)

Output: `tests.toml` at repo root, parse-validated and dry-run-validated against `package_check`.
</objective>

<execution_context>
@C:\Users\Alex\.config\opencode\get-shit-done\workflows\execute-plan.md
@C:\Users\Alex\.config\opencode\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-CONTEXT.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-RESEARCH.md
@manifest.toml

**User decisions (locked — do not revisit):**
- `test_format = 1.0` + `#:schema` header comment; supply `args.admin_email`; **NO `exclude` block**; one `[default.curl_tests]` block (`home.path="/"`, `home.expect_return_code=200`, `home.auto_test_assets=true`); ONE `test_upgrade_from` entry.
- Do NOT add `exclude`; do NOT add `only = [...]` to `[default]` (parser raises `"'only' is not allowed on the default test suite"`); do NOT add a private-install suite (defer to Phase 6).

**RESEARCH CORRECTIONS (higher-confidence than CONTEXT — apply these, and note the correction in task actions):**
- The CONTEXT-named target `0.8.8-rc3~ynh1` (commit `116691c`) is **manifest-only, no `scripts/`** — NOT installable. Verified in this repo: `git ls-tree -r --name-only 116691c | grep '^scripts/'` is empty. Use commit **`05e3d5b`** (version `0.8.8-rc3~ynh2`, 6 scripts present) named "v1.0 (0.8.8-rc3~ynh2)". The schema key pattern is `^[a-z0-9_]*$`, so a bare SHA prefix is required (no `~`/`.`).
- The auto-deduced suite is **`package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url`**. `install.subdir` is suppressed because the manifest has **no `[install.path]` question** (`is_full_domain_app` True). `install.private` is **never** auto-generated by the parser. `install.multi` is skipped (`multi_instance = false`). Do NOT claim subdir/private run.
- `change_url` **will** be auto-generated even though `scripts/change_url` does not exist — expect it to fail; that is a Phase 6 finding (POLS-02 deferred). Do NOT add `exclude` to silence it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests.toml per the locked contents and research corrections</name>
  <files>tests.toml</files>
  <action>
Create `tests.toml` at the repository root with exactly this structure (matching `example_ynh` conventions and `tests.v1.schema.json`):

```toml
#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # LibreChat has no [install.path] question -> package_check auto-deduces only
    # install.root (not install.subdir). multi_instance=false -> no install.multi.
    # install.private is never auto-generated by the parser. No 'exclude' used.

    # admin_email is a text arg with no manifest default; package_check cannot
    # guess it, so it MUST be supplied here or every install test aborts.
    args.admin_email = "package_checker@example.com"

    # Upgrade from the last INSTALLABLE released version (0.8.8-rc3~ynh2).
    # NB: the schema key must match ^[a-z0-9_]*$ and be a valid git ref used by
    # `git checkout --force`. CONTEXT.md named 0.8.8-rc3~ynh1 but that version
    # existed only at commit 116691c, a manifest-only skeleton with no scripts/;
    # it is not installable, so the correct target is commit 05e3d5b.
    test_upgrade_from.05e3d5b.name = "v1.0 (0.8.8-rc3~ynh2)"

    # Curl smoke-tests: SPA HTML serves and its first CSS/JS assets load.
    # sso=false -> no logged_on_sso tests.
    [default.curl_tests]
    home.path = "/"
    home.expect_return_code = 200
    home.auto_test_assets = true
```

Constraints (locked decisions — violating any breaks the phase):
- `test_format = 1.0` must be a TOML **number**, not a string (the parser asserts equality with 1.0).
- The `#:schema` header comment must be present.
- **No `exclude` block.**
- **No `only = [...]` inside `[default]`** — `filter_test_list()` raises on this.
- Exactly one `test_upgrade_from.<sha>` entry, key `05e3d5b` (lowercase hex prefix; the schema rejects `~`/`.`).
- Exactly one `[default.curl_tests]` block with the three dotted `home.*` keys.

Avoid: version strings as `test_upgrade_from` keys, any `[suite]`/`only` additions, `install.private` suites, and any attempt to override `domain`/`path`/`is_public`/`admin`/`password` (package_check auto-fills those).
  </action>
  <verify>
    <automated>
# Preflight: tests.toml exists and contains the exact locked keys.
test -f tests.toml
grep -q '^test_format = 1.0$' tests.toml
grep -q '#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json' tests.toml
grep -q 'args.admin_email = "package_checker@example.com"' tests.toml
grep -q 'test_upgrade_from.05e3d5b.name' tests.toml
grep -q '\[default.curl_tests\]' tests.toml
grep -q 'home.auto_test_assets = true' tests.toml
# Negative checks — these MUST NOT appear:
! grep -q 'exclude' tests.toml
! grep -q '^\[default\].*only' tests.toml
! grep -q 'install.private' tests.toml
# Parse the TOML with python's tomllib and assert the locked values.
python3 - <<'PY' 2>/dev/null || python - <<'PY2'
import tomllib
d = tomllib.load(open("tests.toml","rb"))
assert d["test_format"] == 1.0, d["test_format"]
assert "default" in d
assert d["default"]["args"]["admin_email"] == "package_checker@example.com"
assert "05e3d5b" in d["default"]["test_upgrade_from"]
assert d["default"]["curl_tests"]["home"]["path"] == "/"
assert d["default"]["curl_tests"]["home"]["expect_return_code"] == 200
assert d["default"]["curl_tests"]["home"]["auto_test_assets"] is True
assert "exclude" not in d
print("tests.toml parses and locked keys verified")
PY
PY2
    </automated>
    <manual>Visually confirm the `#:schema` header and that no `exclude`/`only`/private-suite content is present.</manual>
    <sampling_rate>run after this task commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>
tests.toml exists at repo root; parses with Python `tomllib`; contains `test_format = 1.0`, the `#:schema` header, `args.admin_email`, `test_upgrade_from.05e3d5b.name`, and a single `[default.curl_tests]` block; contains no `exclude`, no `only` on `[default]`, and no private-install suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate tests.toml against package_check's parser and dry-run</name>
  <files>tests.toml</files>
  <action>
Validate that `package_check`'s own parser accepts `tests.toml` and deduces the expected suite. This requires a `package_check` clone — if one does not exist yet on this dev box, clone it into a scratch directory OUTSIDE the repo (do NOT commit it), e.g. `git clone --depth 1 https://github.com/YunoHost/package_check /tmp/package_check` (under WSL) or `%TEMP%\package_check`.

Run BOTH validations and capture their output:
1. Parser deduction (fast, no containers):
   `python3 lib/parse_tests_toml.py <REPO_ROOT> | jq`  (from the package_check clone root)
   Confirm the JSON is keyed by suite `"default"` and its test IDs are exactly:
   `package_linter`, `install.root`, `backup_restore`, `upgrade`, `upgrade.05e3d5b`, `change_url`.
   Confirm it does NOT contain `install.subdir` (no `[install.path]` question), `install.private` (never auto-generated), or `install.multi` (`multi_instance = false`).
   Confirm the deduction includes the supplied install arg `admin_email=package_checker@example.com`.
2. Harness dry-run (no containers):
   `./package_check.sh -D <REPO_ROOT>`
   Confirm it prints the test JSON and exits 0 without the `"'only' is not allowed on the default test suite"` exception or any TOML/schema error.

Record the deducation JSON output into `.planning/phases/05-tests-toml-local-package-check-environment/tests-deduction.json` so the suite shape is archived with the phase.

Note in the task log: `change_url` is expected in the deduced list even though `scripts/change_url` does not exist — it will fail at run time and is a Phase 6 finding (POLS-02 deferred). Do NOT respond by adding an `exclude`.
  </action>
  <verify>
    <automated>
PC_DIR="${PC_DIR:-/tmp/package_check}"
test -d "$PC_DIR" || { echo "package_check clone missing at $PC_DIR"; exit 1; }
# 1. Parser accepts tests.toml and deduces the expected suite.
python3 "$PC_DIR/lib/parse_tests_toml.py" "$(pwd)" > .planning/phases/05-tests-toml-local-package-check-environment/tests-deduction.json
jq -e '."default" | has("package_linter") and has("install.root") and has("backup_restore") and has("upgrade") and has("upgrade.05e3d5b") and has("change_url")' .planning/phases/05-tests-toml-local-package-check-environment/tests-deduction.json
jq -e '."default" | (has("install.subdir") | not) and (has("install.private") | not) and (has("install.multi") | not)' .planning/phases/05-tests-toml-local-package-check-environment/tests-deduction.json
# 2. Harness dry-run prints test JSON and exits 0 with no exceptions.
( cd "$PC_DIR" && ./package_check.sh -D "$OLDPWD" ) > .planning/phases/05-tests-toml-local-package-check-environment/tests-dryrun.txt 2>&1
grep -q 'install.root' .planning/phases/05-tests-toml-local-package-check-environment/tests-dryrun.txt
! grep -qi "only.*not allowed on the default" .planning/phases/05-tests-toml-local-package-check-environment/tests-dryrun.txt
echo "parse + dry-run OK"
    </automated>
    <manual>Read tests-deduction.json and confirm the suite shape matches the research-corrected expectation.</manual>
    <sampling_rate>run after this task commits — this is the CI-01 acceptance gate before the full-suite plan (05-03)</sampling_rate>
  </verify>
  <done>
`python3 lib/parse_tests_toml.py <repo> | jq` succeeds and yields exactly `{package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url}` under `default`, with `admin_email` supplied and none of `install.subdir`/`install.private`/`install.multi` present; `./package_check.sh -D <repo>` exits 0 with no exception; both outputs archived in the phase dir.
  </done>
</task>

</tasks>

<verification>
- `tests.toml` exists at repo root and parses as valid TOML (Python `tomllib`).
- Locked keys present: `test_format = 1.0`, `#:schema` header, `args.admin_email`, `test_upgrade_from.05e3d5b.name`, `[default.curl_tests]` with the three `home.*` keys.
- Prohibited content absent: no `exclude`, no `only` on `[default]`, no private-install suite.
- package_check's `parse_tests_toml.py` deduces exactly `package_linter, install.root, backup_restore, upgrade, upgrade.05e3d5b, change_url`.
- `./package_check.sh -D <repo>` exits 0 with no parser exception.
- `tests-deduction.json` and `tests-dryrun.txt` archived in the phase directory.
</verification>

<success_criteria>
CI-01 satisfied: a `tests.toml` test suite exists with `test_format = 1.0`, supplies install args (`args.admin_email`), follows documented syntax, and is accepted by package_check's own parser with the research-corrected suite deduction. No `exclude` misuse.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests-toml-local-package-check-environment/05-01-SUMMARY.md`
</output>
