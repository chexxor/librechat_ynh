---
phase: 03-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [manifest.toml]
autonomous: false
requirements: [UPGR-05]

must_haves:
  truths:
    - "Manifest points at an updated LibreChat source tag with a valid sha256, so `yunohost app upgrade` fetches new source"
    - "Package version string is bumped (~ynhN+1) so YunoHost treats the upgrade as real"
    - "Full live upgrade round: install → edit config → upgrade → config intact, data intact, app working"
  artifacts:
    - path: "manifest.toml"
      provides: "updated main source url/sha256 + version bump"
      contains: "version ="
  key_links:
    - from: "manifest.toml [resources.sources.main]"
      to: "scripts/upgrade"
      via: "ynh_setup_source uses manifest source to fetch the new tag during upgrade"
      pattern: "ynh_setup_source"
---

<objective>
Bump the package to a new LibreChat source tag (sha256-pinned) and run live end-to-end upgrade verification on the user's test server.

Purpose: UPGR-05 — the actual `yunohost app upgrade librechat` proof.
Output: updated manifest.toml + user-confirmed live upgrade.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-upgrade/03-01-SUMMARY.md
@.planning/phases/03-upgrade/03-RESEARCH.md
@manifest.toml
</context>

<tasks>

<task type="auto">
  <name>task 1: Bump manifest to new source tag + version</name>
  <files>manifest.toml</files>
  <action>
    1. Determine target LibreChat tag to pin: read the latest stable/release tag from `https://api.github.com/repos/danny-avila/LibreChat/releases/latest` (field `tag_name`; if no stable release exists, fall back to the latest rc tag listed as gite release). If the fetched tag equals the currently pinned `v0.8.8-rc3` and no newer tag exists, pin the newest tag anyway via the same procedure and note it in the SUMMARY — the exercise still validates the bump flow.
    2. Update `[resources.sources.main]`:
       - `url = "https://github.com/danny-avila/LibreChat/archive/refs/tags/<TAG>.tar.gz"`
       - `sha256` — compute via `curl -sL <url> | sha256sum` (must change from current value; sha256 REQUIRED — never drop the pin, SKEL-05).
       - Keep `in_subdir = true` and the autoupdate strategy lines.
    3. Bump version line: `<upstream_version>~ynh2` (e.g. `0.8.9~ynh2`); upstream_version derived from tag without leading `v` or rc suffixes per YNH conventions (rc tags: keep in version like `0.8.8-rc3~ynh2`).
    4. Cross-check `[resources.nodejs]` version against the new tag's `.nvmrc` (fetch raw `https://raw.githubusercontent.com/danny-avila/LibreChat/<TAG>/.nvmrc`); if it differs from 24, update `[resources.nodejs] version = "<N>"` and note it prominently in SUMMARY (affects install/restore fallback too — only change if it actually differs).
  </action>
  <verify>
    <automated>node -e "const t=require('toml')" 2>/dev/null || python3 -c "import tomllib,sys; tomllib.load(open('manifest.toml','rb'))" 2>/dev/null || (Get-Content manifest.toml | Select-String 'version =|url =|sha256') ; git diff --stat manifest.toml</automated>
    <fallback_automated>git diff manifest.toml shows url AND sha256 AND version changed</fallback_automated>
    <manual>Version bumped, new tag URL, sha256 non-empty and different</manual>
    <sampling_rate>after commit</sampling_rate>
  </verify>
  <done>manifest.toml pins a new tag with fresh sha256 and ~ynh2 version bump; nodejs resource matches upstream .nvmrc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: Live upgrade verification</name>
  <files></files>
  <action>Human performs the live upgrade on their YunoHost test server per the steps in <how-to-verify>. OpenCode cannot execute `yunohost app upgrade` on the user's server.</action>
  <verify>User reports "approved" after completing the checklist; failures reported verbatim</verify>
  <done>User-confirmed: upgrade completes, env/yaml/mongo/meili intact, npm cache stripped, UI reachable</done>
  <what-built>
    Complete upgrade path: scripts/upgrade (Plan 03-01) + bumped manifest (task 1 above).
  </what-built>
  <how-to-verify>
    On the live YunoHost test server (stage as "test" or install repo):
    1. Ensure the app is installed and working; log into the UI, send one test chat message so history and settings exist.
    2. Edit `$install_dir/librechat.yaml` — add an API key and one custom top-level key (e.g. `customSetting: test123`) so merge preservation is testable.
    3. Edit `$install_dir/librechat.env` — append a user-only key line like `MY_CUSTOM_FLAG=1`.
    4. Run `yunohost app upgrade librechat` (with the repo/package updated by this plan). It must complete without errors.
    5. After upgrade verify:
       - `grep MY_CUSTOM_FLAG=1 $install_dir/librechat.env` → present (user key preserved)
       - `grep JWT_SECRET $install_dir/librechat.env` → SAME value as before upgrade (not rotated)
       - `grep 'your-openai-api-key' $install_dir/librechat.yaml` and `grep customSetting $install_dir/librechat.yaml` → both present (yaml never clobbered, merge appended absent keys only as comments)
       - Log into UI; the test chat message from step 1 is still there (MongoDB intact — UPGR-03)
       - If Meilisearch SEARCH enabled: run a search in UI and confirm results (index intact — UPGR-04)
       - `du -sh $install_dir/.npm` → path does not exist (cache strip — success criterion 4)
       - UI reachable at https://DOMAIN post-upgrade.
    6. Report any failures verbatim.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- manifest.toml version bumped with fresh sha256/tag
- Live upgrade completed, user config + mongo data + meili index intact, npm cache stripped
</verification>

<success_criteria>
User confirms `yunohost app upgrade librechat` works end-to-end preserving everything; UPGR-05 verified live (mirrors Phase 2 round-trip approval pattern).
</success_criteria>

<output>
After completion, create `.planning/phases/03-upgrade/03-02-SUMMARY.md`
</output>
