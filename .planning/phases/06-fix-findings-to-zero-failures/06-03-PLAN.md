---
phase: 06-fix-findings-to-zero-failures
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
  - .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/
autonomous: false
requirements: [POLS-01]
must_haves:
  truths:
    - "After the Wave 1 fixes, a full package_check suite runs on the VM and its per-test verdicts are retrieved"
    - "Every finding from cycle 1 is classified into exactly one bucket and recorded in the triage log"
    - "Any flake claim cites a concrete external cause from the log; otherwise the finding is a regression"
  artifacts:
    - path: ".planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/"
      provides: "Cycle-1 run artifacts (Test_results.log, package_check-full.log, full_log_0.log)"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md"
      provides: "Cycle-1 triage entries with buckets"
  key_links:
    - from: ".planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/Test_results.log"
      to: "triage/triage-log.md"
      via: "per-test verdict banners transcribed into the cycle section"
      pattern: "--- (SUCCESS|FAIL) ---"
---

<objective>
Run the first full `package_check` suite after the Wave 1 fixes (headline bug + token sweep + secret hygiene) and triage every finding.

Purpose: Tests 3–6 currently FAIL only as cascades from the install bug; this cycle reveals their true status and any new findings. It converts the static Wave 1 work into empirical evidence.
Output: Cycle-1 artifacts archived; triage-log cycle-1 section complete; a prioritized list of remaining package bugs (if any).
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-findings-to-zero-failures/06-CONTEXT.md
@.planning/phases/06-fix-findings-to-zero-failures/06-RESEARCH.md
@.planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md
@.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md
@doc/PACKAGE_CHECK.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Sync Wave-1 fixes to the VM and run a full package_check cycle</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/</files>
  <action>
    Drive one FULL `package_check` cycle on the Hyper-V Debian 12 VM, per `doc/PACKAGE_CHECK.md` §4 and 06-RESEARCH.md "Loop Mechanics".

    Pre-flight (environment side, from 05-FINDINGS.md §5 — fix if missing, note if applied):
    - Ensure `python3-toml`, linter deps (`python3-jsonschema python3-packaging python3-pyparsing python3-six`), `tmux`, `ethtool` are installed on the VM.
    - Ensure the `yunohost-bookworm-stable-appci` image alias is pinned locally:
      `incus image copy yunohost:91abc4fc4c43 local: --alias yunohost-bookworm-stable-appci`
    - Ensure `eth0-watchdog.service` is active/enabled (NIC-flap mitigation).
    - Clean any stale lock: `./package_check.sh -s`; if it hangs, `pkill -f package_check.sh` and remove `~/package_check/pcheck-*.lock`.

    Run:
    ```powershell
    $vm = "<user>@<static-ip>"   # substitute the environment's SSH target
    ssh -i $env:USERPROFILE\.ssh\id_rsa $vm "rm -rf ~/librechat_ynh && mkdir -p ~/librechat_ynh"
    scp -r -i $env:USERPROFILE\.ssh\id_rsa .\* "${vm}:~/librechat_ynh/"
    ssh -i $env:USERPROFILE\.ssh\id_rsa $vm "cd ~/package_check && tmux new-session -d -s pc './package_check.sh ~/librechat_ynh 2>&1 | tee ~/pc_run_cycle1.log'"
    ```
    Wait for completion (expect ~4–5 min). A completed run prints a Global summary and cleanly exits; a crash/timeout shows a `Critical:` abort with no Global summary (that is an environment failure needing a re-run, not a package finding — but the install tests themselves are long: a full install cycle can take 3–5 min per install test, so allow generous time and prefer `ssh -o ServerAliveInterval=30`).

    Retrieve artifacts into `.planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/`:
    - `Test_results.log`
    - `package_check-full.log` (from `~/pc_run_cycle1.log`)
    - `full_log_0.log`

    Record run metadata in the triage log cycle-1 section: date, command, duration, exit code, git rev of the synced package.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/Test_results.log && grep -q 'Global working time' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/Test_results.log && grep -qE 'Install on root.*(SUCCESS|FAIL)' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-1/Test_results.log</automated>
    <manual>Confirm the run printed a Global summary (completed) rather than a Critical abort. If it aborted on environment grounds, classify as environment and re-run (do not record as a package finding).</manual>
    <sampling_rate>run after this task completes, before triage</sampling_rate>
  </verify>
  <done>Cycle-1 artifacts retrieved; run completed with a Global summary; run metadata recorded.</done>
</task>

<task type="auto">
  <name>task 2: Triage every cycle-1 finding into the four buckets</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md</files>
  <action>
    Triage cycle 1 using 06-RESEARCH.md "Triage classification" and the locked flake rule.

    Extract per-test verdicts from `cycles/cycle-1/Test_results.log` (the `--- SUCCESS ---`/`--- FAIL ---` banners and the per-test summary lines) for all 6 tests: `install.root`, `backup_restore`, `upgrade`, `upgrade.05e3d5b`, `package_linter`, `change_url`.

    For each non-SUCCESS verdict, dig into `full_log_0.log` for the concrete cause and assign exactly one bucket:
    1. **Package bug (in-scope)** — affects an in-scope test → actionable fix (file + symptom).
    2. **Exempt** — `change_url` (POLS-02) or catalog-metadata linter items → cross-reference `06-SCOPE-EXEMPTIONS.md`.
    3. **Environment/harness** — concrete external cause that is not a package defect (apt/CDN/DNS timeout, SSH drop, NIC flap, `imgkit` renderer abort). Quote the log line.
    4. **Flake** — only if (a) a concrete external cause is quoted AND (b) it passes on re-run with NO code change. Do NOT label a failure a flake just because it is inconvenient.

    Write the cycle-1 findings table into `triage/triage-log.md`: | Finding | Test | Bucket | Evidence | Action |. Use exact quoted log evidence. Note any test that remains a cascade (blocked by an earlier install failure) explicitly.

    Produce a prioritized list of remaining in-scope package bugs for Plan 04. If there are none and all in-scope tests are SUCCESS, note "zero in-scope failures — proceed to final archive (Plan 05)".
  </action>
  <verify>
    <automated>grep -q '## Cycle 1' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -qE 'install.root *=' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -qE 'upgrade.05e3d5b *=' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md</automated>
    <manual>Review the findings table: every non-SUCCESS verdict has a bucket and quoted evidence.</manual>
    <sampling_rate>run after triage completes</sampling_rate>
  </verify>
  <done>Cycle-1 section complete with all 6 verdicts, every finding bucketed with quoted evidence, and a prioritized remaining-bug list (or an explicit zero-in-scope-failures confirmation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: User verifies the cycle-1 run and triage</name>
  <files></files>
  <action>
    Present the cycle-1 run and triage to the user. All automated work (run + triage) is already done; this checkpoint confirms the run completed and the triage is sound. Use the <what-built> / <how-to-verify> content below as the prompt.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — no automated command; human confirmation is the gate"</automated>
    <manual>User confirms the run completed, all 6 verdicts are recorded, change_url is classified exempt, and the remaining-bug list is actionable.</manual>
    <sampling_rate>gate before entering the fix loop</sampling_rate>
  </verify>
  <what-built>Cycle-1 run + triage</what-built>
  <how-to-verify>
    Review `cycles/cycle-1/Test_results.log` and the cycle-1 triage table.
    Confirm:
    1. The run completed (Global summary present).
    2. Each in-scope test's verdict is recorded.
    3. The `change_url` FAIL is classified exempt (not a package bug).
    4. The remaining-bug list looks actionable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Cycle-1 artifacts exist and the run completed.
- Triage log has all 6 verdicts and bucketed findings with evidence.
- `change_url` classified exempt.
</verification>

<success_criteria>
- Cycle-1 full-suite results obtained and triaged.
- A clear, prioritized list of remaining in-scope package bugs (possibly empty).
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-findings-to-zero-failures/06-03-SUMMARY.md`
</output>
