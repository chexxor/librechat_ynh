---
phase: 06-fix-findings-to-zero-failures
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/
  - .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [POLS-01]
must_haves:
  truths:
    - "A single full-suite run with zero in-scope failures and exit code 0 is archived as POLS-01 evidence"
    - "The archived logs are redacted — no real secret value appears in the committed evidence"
    - "The archive is one clean run, not a composite of per-test retries"
    - "The Phase 6 scope-exemption record accompanies the evidence"
  artifacts:
    - path: ".planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/Test_results.log"
      provides: "Single clean full-suite run with per-test verdicts"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/REDACTION-REPORT.txt"
      provides: "Proof that redaction found zero residual secret matches"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/RUN-METADATA.md"
      provides: "Provenance: date, command, git rev, duration, exit code, flake-vs-regression note"
  key_links:
    - from: ".planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/Test_results.log"
      to: "06-SCOPE-EXEMPTIONS.md"
      via: "the change_url FAIL is explained by the exemption record"
      pattern: "change_url"
    - from: "scripts/redact_pc_logs.sh"
      to: "pols-01-evidence/"
      via: "redaction + assert-only over the archive"
      pattern: "REDACTION OK"
---

<objective>
Perform the FINAL single clean full-suite run, redact and archive it as POLS-01 live verification, and update the project state/roadmap/requirements.

Purpose: POLS-01 requires a *single* clean run (not a retry composite), archived cleanly. This plan produces the phase's headline deliverable and closes the phase.
Output: `pols-01-evidence/` (redacted logs + metadata + redaction report); updated STATE/ROADMAP/REQUIREMENTS.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-fix-findings-to-zero-failures/06-CONTEXT.md
@.planning/phases/06-fix-findings-to-zero-failures/06-RESEARCH.md
@.planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md
@.planning/phases/06-fix-findings-to-zero-failures/06-04-SUMMARY.md
@.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Run the final single clean full-suite cycle</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/</files>
  <action>
    Confirm the working tree is clean and the code is identical to the last passing cycle (`git status`, `git rev-parse HEAD`) — the final run must have **no code change** relative to the verified state.

    Run ONE full `package_check` suite (same procedure as Plan 03 Task 1) and retrieve artifacts into `cycles/cycle-final/`:
    - `Test_results.log`
    - `package_check-full.log`
    - `full_log_0.log`

    Record: date/time, command, git rev, duration, `PACKAGE_CHECK_EXIT` value. The run MUST show:
    - A Global summary (completed, no `Critical:` abort, no crash/timeout).
    - Zero in-scope FAIL/ERROR: `install.root`, `backup_restore`, `upgrade`, `upgrade.05e3d5b`, `package_linter` all SUCCESS.
    - `change_url` FAIL allowed (exempt).
    - Exit code 0.

    If any in-scope test FAILs, STOP — this is not the clean run. If the failure has a concrete external cause, apply the locked flake rule (≤2 no-change re-runs); a persistent failure returns to Plan 04. Do NOT archive a failing or retry-composite run as POLS-01 evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/Test_results.log && grep -q 'Global working time' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/Test_results.log && grep -qE 'Install on root.*SUCCESS' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/Test_results.log && grep -qE 'Backup/restore.*SUCCESS' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/Test_results.log && grep -qE 'Upgrade.*SUCCESS' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-final/Test_results.log</automated>
    <manual>Confirm exit code 0 and that change_url is the only FAIL. Confirm no code changed since the previous passing cycle.</manual>
    <sampling_rate>final run — gate for the whole phase</sampling_rate>
  </verify>
  <done>A single full-suite run completed with zero in-scope failures and exit code 0, with no code change since the verified state.</done>
</task>

<task type="auto">
  <name>task 2: Redact and archive the POLS-01 evidence</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/</files>
  <action>
    Create `.planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/` and populate it:

    1. Assemble the list of real secret values generated in the run (from the run's app settings on the VM, or by extracting from the raw `full_log_0.log` / `package_check-full.log`; at minimum `admin_password`, `admin_panel_secret`, `jwt_secret`, `jwt_refresh_secret`, `meili_master_key`, `mongopwd`). Write them to a scratch `secrets.txt` (KEY=VALUE per line) OUTSIDE the committed evidence dir; never commit it.

    2. Redact the three logs into the evidence dir using `scripts/redact_pc_logs.sh` with `SECRETS_FILE`:
       - `pols-01-evidence/Test_results.log`
       - `pols-01-evidence/package_check-full.log`
       - `pols-01-evidence/full_log_0.log`
       Capture the tool's output as `pols-01-evidence/REDACTION-REPORT.txt` (must contain `REDACTION OK: 0 residual secret matches`).

    3. Run the assertion-only mode over the evidence dir to double-check: `bash scripts/redact_pc_logs.sh --assert-only pols-01-evidence/` with the same secrets.

    4. Write `pols-01-evidence/RUN-METADATA.md` with provenance:
       - Date/time, host/VM, package git rev, command, duration, `PACKAGE_CHECK_EXIT: 0`.
       - Per-test verdict table for all 6 tests, marking `change_url` as exempt with a link to `06-SCOPE-EXEMPTIONS.md`.
       - Flake-vs-regression note (confirm the final run had no code change relative to the verified state; note any environment tweak).
       - The provenance note that `Test_results.log` is a synthesized de-ANSI'd capture (upstream emits `full_log_0.log`/`results_0.json`/`summary_0.png`), carried over from Phase 5.
       - Explicit statement: this is Phase 6's OWN single clean run; the Phase 5 findings run is not a zero-failure claim.

    5. Delete the scratch secrets file. Ensure the unredacted raw logs are NOT committed.

    Then update project records:
    - `.planning/REQUIREMENTS.md`: mark POLS-01 `[x]`; update the traceability table (POLS-01 Phase 6 → Complete). Also mark CI-01 if now satisfied (tests.toml exists + parse-validated) — check current status; only mark if true.
    - `.planning/STATE.md`: current position → Phase 6 complete; last activity note; progress %; move the Phase 6 blockers to resolved.
    - `.planning/ROADMAP.md`: Phase 6 checkbox `[x]`, plans list, progress table row → Complete with date.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/Test_results.log && test -f .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/REDACTION-REPORT.txt && test -f .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/RUN-METADATA.md && grep -q 'REDACTION OK' .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/REDACTION-REPORT.txt && ! git ls-files --error-unmatch .planning/phases/06-fix-findings-to-zero-failures/pols-01-evidence/secrets.txt 2>/dev/null && grep -q 'POLS-01' .planning/REQUIREMENTS.md</automated>
    <manual>Spot-check the redacted Test_results.log: the `ADMIN_PANEL_SESSION_SECRET=` line (if present) shows `***REDACTED***`, and the intended admin-password display is masked if it appears. Confirm RUN-METADATA.md marks change_url exempt.</manual>
    <sampling_rate>one-time archive step</sampling_rate>
  </verify>
  <done>Redacted evidence archived with metadata and a zero-residual redaction report; scratch secrets not committed; STATE/ROADMAP/REQUIREMENTS updated for POLS-01 completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: User accepts the POLS-01 clean-run archive and project records</name>
  <files></files>
  <action>
    Present the redacted POLS-01 evidence and the updated project records to the user. All automated work (final run + redaction + record updates) is already done; this checkpoint is the final phase gate. Use the <what-built> / <how-to-verify> content below as the prompt.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — no automated command; human confirmation is the gate"</automated>
    <manual>User confirms the redacted logs contain no real secrets, show all in-scope tests SUCCESS with change_url exempt, the metadata is accurate, and the records reflect POLS-01 complete.</manual>
    <sampling_rate>final gate of the phase</sampling_rate>
  </verify>
  <what-built>POLS-01 clean-run archive + project records</what-built>
  <how-to-verify>
    Review `pols-01-evidence/`:
    1. `Test_results.log` shows all in-scope tests SUCCESS and `change_url` FAIL (exempt).
    2. `REDACTION-REPORT.txt` says zero residual secret matches.
    3. No real secret values are visible in the redacted logs.
    4. `RUN-METADATA.md` provenance is accurate and states this is a single clean run.
    Confirm the requirements/state/roadmap now reflect POLS-01 complete.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pols-01-evidence/` contains redacted logs + metadata + redaction report.
- Redaction report asserts zero residuals.
- No secrets committed; raw logs excluded.
- STATE/ROADMAP/REQUIREMENTS updated.
</verification>

<success_criteria>
- POLS-01 live verification archived: one clean, redacted full-suite run with zero in-scope failures.
- Phase 6 records updated and the exemption record linked.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-findings-to-zero-failures/06-05-SUMMARY.md`
</output>
