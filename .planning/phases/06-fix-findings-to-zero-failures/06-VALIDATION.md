---
phase: 6
slug: fix-findings-to-zero-failures
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-09-20
---

# Phase 6 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd-plan-phase 6`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd-execute-phase 6`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | YunoHost `package_check` (`f7d32ca`) on the Hyper-V Debian 12 + Incus VM; `package_linter` locally (WSL2/WSL) for cheap pre-flight |
| **Config file** | `tests.toml` (FROZEN — md5 `4cdd83e3b8e13b08c3aac8e5e79ab07d`); local linter wrapper `scripts/run_lint.sh` |
| **Quick run command** | `wsl -e bash -lc 'bash scripts/run_lint.sh'` (static pre-flight, ~20–40 s) |
| **Full suite command** | VM, SSH-driven: `cd ~/package_check && ./package_check.sh ~/librechat_ynh` (~4–5 min) |
| **Estimated runtime** | ~4–5 min per full-suite cycle; linter ~20–40 s |
| **CI pipeline** | none for Phase 6 — GH Actions lint workflow is Phase 7 (GHCI-01) |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit (fix set):** Run the quick static gate `wsl -e bash -lc 'bash scripts/run_lint.sh'` — no VM cycle wasted on a lint regression.
- **After every plan wave / fix set:** Run the **full** `package_check` suite (locked decision — full suite every cycle, no targeted re-runs).
- **Before `/gsd-verify-work`:** Single clean full-suite run with zero in-scope FAILs + exit code 0; redacted archive in place.
- **Maximum acceptable task feedback latency:** ~5 min (accepted per locked decision).

---

## Per-task Verification Map

| task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 06-01-01 | 01 | 1 | POLS-01 | static | `grep -oE '__[A-Z0-9_]+__' conf/* \| ...` reconcile against scope map — no token without a matching bash var | ❌ W0 | ⬜ pending |
| 06-01-02 | 01 | 1 | POLS-01 | static | `wsl -e bash -lc 'bash scripts/run_lint.sh'` green after headline fix | ✅ N/A | ⬜ pending |
| 06-02-01 | 02 | 1 | POLS-01 | static | grep-assert `set +o xtrace` guards wrap secret generation; exactly one intended admin-password print | ❌ W0 | ⬜ pending |
| 06-02-02 | 02 | 1 | POLS-01 | contract | redaction script: 0 known-secret matches in redacted archive | ❌ W0 | ⬜ pending |
| 06-03-01 | 03 | 2 | POLS-01 | e2e | VM full suite cycle 1 — `Test_results.log` per-test banners | ✅ N/A | ⬜ pending |
| 06-03-02 | 03 | 2 | POLS-01 | e2e | triage all findings into buckets (package bug / exempt / environment / flake) | ❌ W0 | ⬜ pending |
| 06-04-01 | 04 | 3 | POLS-01 | e2e | VM full suite cycles until zero in-scope FAILs + exit 0 | ✅ N/A | ⬜ pending |
| 06-04-02 | 04 | 3 | POLS-01 | policy | `06-SCOPE-EXEMPTIONS.md` records change_url + catalog + subpath/private | ❌ W0 | ⬜ pending |
| 06-05-01 | 05 | 4 | POLS-01 | e2e | FINAL single clean full-suite run archived (redacted) | ✅ N/A | ⬜ pending |
| 06-05-02 | 05 | 4 | POLS-01 | contract | `PACKAGE_CHECK_EXIT: 0` + Global summary present; redacted archive assertions pass | ❌ W0 | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Artifact/process scaffolding committed BEFORE the fix loop. Executor runs Wave 0 first.
> No conventional unit-test framework exists — the `package_check` suite IS the test.

- [ ] **Placeholder→variable reconciliation check** — encode the RESEARCH.md map as a static assertion/checklist so latent `__TOKEN__` mismatches are caught without a VM cycle.
- [ ] **Redaction script** — extracts known secret values from a run and masks all occurrences in the archived logs; grep-asserts zero residual matches.
- [ ] **Triage log template** — per-cycle finding table with bucket column (package bug / exempt / environment / flake) and flake-vs-regression evidence fields.
- [ ] **`06-SCOPE-EXEMPTIONS.md`** — Phase 6 scope-exemption record (change_url POLS-02; catalog metadata; uncovered subpath/private/multi).
- [ ] **Redacted POLS-01 archive directory** — target location/naming for the single clean run's artifacts.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| User-operated VM/SSH provisioning + suite launch | POLS-01 | The Hyper-V VM and SSH credentials are user-owned infrastructure; the orchestrator drives over SSH but cannot provision the VM | Confirm VM reachable (`ssh -i ~/.ssh/id_rsa <user>@<ip> uname -a`), run the full suite under `tmux`, retrieve artifacts |
| Final POLS-01 run legitimacy (single clean run, not a retry composite) | POLS-01 | Requires human judgment that no code change occurred between the final run and its predecessor retries | Confirm `git` has no diff since the last passing cycle; confirm the archived run is one full-suite invocation with exit 0 |

*All other phase behaviors have automated verification coverage (suite verdict banners, exit code, grep assertions).*

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 300s ✅ (static gates < 60s; VM full-suite cycles are the accepted locked cost)
- [x] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** approved on 2026-09-20 (all 5 plans pass `verify plan-structure`; requirement POLS-01 covered in every plan; dependency graph acyclic with waves 1→2→3→4; context compliance confirmed; Dimension 8 pass — one non-blocking latency note: the VM full-suite cycle runs ~4–5 min, accepted per locked decision)

---

## Execution Tracking

Updated during `/gsd-execute-phase 6`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | 5 | static reconciliation | — | — | scaffold |
| 1 | 2 | `run_lint.sh` + grep asserts | — | — | ⬜ pending |
| 2 | 2 | VM full suite (cycle 1) | — | — | ⬜ pending |
| 3 | 2 | VM full suite (fix loop) | — | — | ⬜ pending |
| 4 | 2 | VM full suite (final clean) | — | — | ⬜ pending |

**Phase validation complete:** pending
