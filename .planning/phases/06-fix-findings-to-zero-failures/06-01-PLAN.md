---
phase: 06-fix-findings-to-zero-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conf/librechat.env
  - .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md
  - .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
autonomous: true
requirements: [POLS-01]
must_haves:
  truths:
    - "Every __TOKEN__ in every conf template has a matching lowercase bash variable in scope at its ynh_config_add call site (no uninitialized-variable install abort)"
    - "A Phase 6 scope-exemption record exists documenting change_url (POLS-02), catalog metadata, and uncovered subpath/private/multi tests"
    - "A triage log template exists with four finding buckets (package bug / exempt / environment / flake)"
  artifacts:
    - path: "conf/librechat.env"
      provides: "Admin panel secret token that resolves to the generated admin_panel_secret variable"
      contains: "__ADMIN_PANEL_SECRET__"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md"
      provides: "Phase 6 out-of-scope-by-design finding record"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md"
      provides: "Per-cycle finding triage template"
  key_links:
    - from: "conf/librechat.env"
      to: "scripts/_common.sh"
      via: "ynh_config_add token resolution"
      pattern: "__ADMIN_PANEL_SECRET__"
---

<objective>
Wave 0/1 static groundwork: reconcile every conf template token against the bash variable scope (the headline fix plus a full-token sweep so no latent mismatch survives to a VM cycle), create the Phase 6 scope-exemption record, and create the triage log template.

Purpose: The headline bug (`__ADMIN_PANEL_SESSION_SECRET__` → never-set `$admin_panel_session_secret`) aborts every install. Fixing it — and pre-emptively verifying every other `__TOKEN__` — turns the first VM cycle from a discovery run into a validation run. The exemption record and triage template make the loop auditable per locked decisions.
Output: Fixed `conf/librechat.env`; `06-SCOPE-EXEMPTIONS.md`; `triage/triage-log.md`.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-findings-to-zero-failures/06-CONTEXT.md
@.planning/phases/06-fix-findings-to-zero-failures/06-RESEARCH.md
@.planning/phases/05-tests-toml-local-package-check-environment/05-FINDINGS.md
@.planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Reconcile every conf template token and fix the headline admin-panel-secret mismatch</name>
  <files>conf/librechat.env</files>
  <action>
    Implement the headline fix using **Option A** from 06-RESEARCH.md (change the template token, not the variable/setting name — least risk, no settings migration, upgrade/restore stay consistent):

    1. In `conf/librechat.env`, change line 26 from
       `ADMIN_PANEL_SESSION_SECRET=__ADMIN_PANEL_SESSION_SECRET__`
       to
       `ADMIN_PANEL_SESSION_SECRET=__ADMIN_PANEL_SECRET__`
       Rationale: YunoHost's `_ynh_replace_vars` lowercases the token and requires `$admin_panel_secret`, which is generated and persisted by `librechat_generate_secrets` (`scripts/_common.sh:66-67`) and read back at `scripts/_common.sh:127,131`. The emitted env KEY (`ADMIN_PANEL_SESSION_SECRET=`) stays unchanged — that is LibreChat's contract. Only the `__TOKEN__` changes.

    2. Full-token sweep (prevent latent mismatches WITHOUT a VM cycle). Enumerate every `__TOKEN__` across ALL `conf/*` templates and verify its lowercased name is a bash variable in scope at the corresponding `ynh_config_add` call site. The authoritative map is the "Complete Placeholder → Variable Map" section of 06-RESEARCH.md. Re-verify it against the current files (the map was derived on 2026-09-20):
       - `conf/librechat.env`: `__PORT__`→$port, `__DOMAIN__`→$domain, `__DB_USER__`→$db_user, `__DB_PWD__`→$db_pwd, `__DB_NAME__`→$db_name, `__MEILI_MASTER_KEY__`→$meili_master_key, `__JWT_SECRET__`→$jwt_secret, `__JWT_REFRESH_SECRET__`→$jwt_refresh_secret, `__ADMIN_PANEL_SECRET__`→$admin_panel_secret (fixed here)
       - `conf/librechat.service`: `__APP__`, `__INSTALL_DIR__`, `__NODEJS_DIR__`, `__DATA_DIR__` — all YNH init vars
       - `conf/meilisearch.service`: `__APP__`, `__MEILI_MASTER_KEY__`, `__DATA_DIR__`
       - `conf/nginx.conf`: `__PATH__`, `__PORT__`
       - `conf/librechat.yaml`: no tokens
       Use grep to extract tokens: `grep -ohE '__[A-Z0-9_]+__' conf/*`. For each token NOT in the known-good set, find its provider variable or flag it. Note: `admin_password` is NOT a template placeholder — confirmed absence is expected and correct (do NOT add a token).

    3. Confirm no `__ADMIN_PANEL_SESSION_SECRET__` remains anywhere in `conf/`: `grep -rn '__ADMIN_PANEL_SESSION_SECRET__' conf/` must return empty.

    Do NOT change `tests.toml`. Do NOT rename the `admin_panel_secret` variable or setting key. Do NOT touch `scripts/` in this task.
  </action>
  <verify>
    <automated>grep -q 'ADMIN_PANEL_SESSION_SECRET=__ADMIN_PANEL_SECRET__' conf/librechat.env && ! grep -rq '__ADMIN_PANEL_SESSION_SECRET__' conf/ && test "$(grep -ohE '__[A-Z0-9_]+__' conf/* | sort -u | grep -vE '^(__APP__|__INSTALL_DIR__|__DATA_DIR__|__NODEJS_DIR__|__PORT__|__DOMAIN__|__PATH__|__DB_USER__|__DB_PWD__|__DB_NAME__|__MEILI_MASTER_KEY__|__JWT_SECRET__|__JWT_REFRESH_SECRET__|__ADMIN_PANEL_SECRET__)$' | wc -l)" -eq 0</automated>
    <manual>Confirm the emitted env line still reads `ADMIN_PANEL_SESSION_SECRET=` (key unchanged) with only the token changed.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`conf/librechat.env:26` uses `__ADMIN_PANEL_SECRET__`; no `__ADMIN_PANEL_SESSION_SECRET__` remains in `conf/`; every remaining token is in the known-good set (sweep command returns 0 unexpected tokens).</done>
</task>

<task type="auto">
  <name>task 2: Create the Phase 6 scope-exemption record</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md</files>
  <action>
    Create `.planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md`, modeled on `.planning/phases/04-lint-baseline/04-SCOPE-EXEMPTIONS.md`. It must define the phase's zero-failure invariant and name every out-of-scope-by-design item with a concrete reason and the change that would unblock it.

    Required content:
    - **Purpose** — mirror the 04 preamble (honest, auditable zero-failure claim).
    - **Zero-Failure Invariant** — encode the locked definition: an in-scope test set = {`install.root`, `backup_restore`, `upgrade`, `upgrade.05e3d5b`, `package_linter`}; zero in-scope FAIL/ERROR + `package_check` exit code 0. `change_url` FAIL is excluded by policy.
    - **Exempted Findings table** with columns Finding | Bucket | Reason exempt | Unblocked by:
      - `change_url` FAIL — no `scripts/change_url` exists — deferred v2 / POLS-02 (REQUIREMENTS.md v2) — implementing the script (out of Phase 6 scope)
      - `AppCatalog.is_in_catalog` (critical) — catalog submission PR out of scope — future catalog PR
      - `AppCatalog.state_is_working` (error) — same root cause — future catalog PR
      - `AppCatalog.has_category` (warning) — same root cause — future catalog PR
      - Uncovered `install.subdir` — no `[install.path]` question, parser treats app as domain-root — adding `[install.path]` (future phase)
      - Uncovered `install.private` — parser never auto-generates a private suite — explicit extra suite (future phase)
      - Skipped `install.multi` — `multi_instance = false` in manifest — POLS-03 (multi-instance blocked by single-instance Meilisearch wiring)
    - **Not Exempted** section noting the actual in-scope fixes are tracked in the triage log/plans.
    - **Evidence-separation note** — Phase 6 archives its OWN clean run; the Phase 5 findings run is not a zero-failure claim.

    Do NOT add an `exclude` to `tests.toml` (locked: no fake green).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md && grep -q 'change_url' .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md && grep -q 'AppCatalog.is_in_catalog' .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md && grep -q 'install.subdir' .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md && grep -q 'install.private' .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md && grep -q 'install.multi' .planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The exemption record exists with the zero-failure invariant and all 7 exempted/uncovered items tabulated with reasons and unblocking actions.</done>
</task>

<task type="auto">
  <name>task 3: Create the triage log template</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md</files>
  <action>
    Create `.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md`, the per-cycle finding record for the fix→run→triage loop.

    Structure:
    - Header explaining the loop and the four buckets.
    - Bucket definitions (from 06-RESEARCH.md "Triage classification"):
      1. **Package bug (in-scope)** — FAIL/ERROR on an in-scope test → FIX.
      2. **Exempt** — `change_url` (POLS-02) or catalog-metadata linter items → DOCUMENT only.
      3. **Environment/harness** — e.g. `imgkit` renderer abort, apt/CDN/DNS timeout with external evidence → DOCUMENT/re-run, not a package defect.
      4. **Flake** — FAIL with concrete external cause AND passes on re-run with NO code change → DIAGNOSTIC only.
    - A per-cycle section template:
      ```
      ## Cycle N — YYYY-MM-DD
      - Run: <command>, duration, exit code
      - Code state (git rev): <sha>, clean/dirty
      - Test verdicts: install.root=<>, backup_restore=<>, upgrade=<>, upgrade.05e3d5b=<>, package_linter=<>, change_url=<>
      - Findings table: | Finding | Test | Bucket | Evidence | Action |
      - Flake-vs-regression: <external cause quoted from log OR "no external cause → regression">
      ```
    - Seed a Cycle 0 row noting the Phase 5 baseline (install.root FAIL; other in-scope cascaded FAILs; package_linter SUCCESS; change_url FAIL exempt).
    - A note: POLS-01 archive must be a SINGLE clean run, never a composite of retries.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -q 'Package bug' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -q 'Environment/harness' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -q 'Flake' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && grep -q 'Cycle 0' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The triage log exists with the four bucket definitions, a reusable per-cycle section, and the Phase 5 baseline seeded as Cycle 0.</done>
</task>

</tasks>

<verification>
- `grep -rq '__ADMIN_PANEL_SESSION_SECRET__' conf/` returns empty.
- Token sweep returns zero unexpected tokens.
- `06-SCOPE-EXEMPTIONS.md` and `triage/triage-log.md` exist with required content.
- No change to `tests.toml` (`git diff --quiet tests.toml`).
</verification>

<success_criteria>
- Headline bug fixed via the least-risk one-token change; emitted env key unchanged.
- Every conf token statically reconciled against variable scope.
- Scope-exemption record and triage template created for the loop.
- `tests.toml` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-findings-to-zero-failures/06-01-SUMMARY.md`
</output>
