---
phase: 06-fix-findings-to-zero-failures
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - scripts/install
  - scripts/upgrade
  - scripts/restore
  - scripts/backup
  - scripts/_common.sh
  - conf/librechat.env
  - conf/librechat.service
  - conf/meilisearch.service
  - conf/nginx.conf
  - .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
  - .planning/phases/06-fix-findings-to-zero-failures/cycles/
autonomous: false
requirements: [POLS-01]
must_haves:
  truths:
    - "Every in-scope package bug surfaced by triage is fixed and the fix is justified against the quoted evidence"
    - "Each fix set is followed by a FULL suite re-run and re-triage (no targeted re-runs)"
    - "A persistent failure after 2 retries is treated as a regression, not a flake"
    - "The final state is zero in-scope FAIL/ERROR"
  artifacts:
    - path: ".planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md"
      provides: "Cycle 2..N fix/run/triage history"
    - path: ".planning/phases/06-fix-findings-to-zero-failures/cycles/"
      provides: "Per-cycle artifacts"
  key_links:
    - from: "triage/triage-log.md"
      to: "scripts/*"
      via: "each package-bug finding maps to a specific fix"
      pattern: "package bug"
    - from: "cycles/cycle-N/Test_results.log"
      to: "triage/triage-log.md"
      via: "verdicts re-transcribed each cycle"
      pattern: "## Cycle"
---

<objective>
Iterate the fix→run→triage loop until every in-scope test reaches SUCCESS: fix each package bug surfaced by Plan 03's triage, re-run the FULL suite, re-triage, repeat.

Purpose: Zero failures is the milestone deliverable; install.root passing unblocks the previously-cascaded tests, whose true statuses drive further fixes. This plan is the core of Phase 6.
Output: Fixed package source; cycle 2..N artifacts; triage log showing convergence to zero in-scope failures.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-findings-to-zero-failures/06-CONTEXT.md
@.planning/phases/06-fix-findings-to-zero-failures/06-RESEARCH.md
@.planning/phases/06-fix-findings-to-zero-failures/06-SCOPE-EXEMPTIONS.md
@.planning/phases/06-fix-findings-to-zero-failures/06-03-SUMMARY.md
@.planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md
@doc/PACKAGE_CHECK.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix the remaining in-scope package bugs from triage</name>
  <files>scripts/* (as indicated by triage), conf/* (as indicated by triage)</files>
  <action>
    Address every finding bucketed **package bug (in-scope)** in the triage log. Use the quoted evidence as the specification; do not speculate beyond it.

    Known-likely fixes (from 06-RESEARCH.md "What each in-scope test exercises") — fix only those actually surfaced by triage:
    - **backup_restore:** if `conf/meilisearch.service` fails to render on restore because `meili_master_key` is not in scope, add an explicit read-back in `scripts/restore` (`meili_master_key=$(ynh_app_setting_get --key=meili_master_key)`) before `ynh_config_add_systemd --template=meilisearch.service`, mirroring `librechat_regen_and_merge_configs` in `_common.sh`.
    - **upgrade (same version):** verify `ynh_setup_source --keep="librechat.env librechat.yaml"` preserves configs; if the merge clobbers user keys or rotates secrets, fix `librechat_regen_and_merge_configs`.
    - **upgrade.05e3d5b:** if upgrade from the old release dies in the missing-setting validation loop (`_common.sh:131-135`), handle the legacy settings shape (e.g. tolerate a missing optional setting, or map the old key) rather than aborting.
    - **install.root residual:** any second template/variable mismatch, service start failure, nginx error, create-user.js failure, or admin-email failure surfaced by the log.

    Rules:
    - Do NOT touch `tests.toml` (frozen). Do NOT add `exclude`. Do NOT implement `scripts/change_url` (POLS-02).
    - Do NOT chase environment/harness findings (e.g. `imgkit`) as package bugs.
    - After edits, run the cheap static gate before the VM cycle: `wsl -e bash -lc 'bash scripts/run_lint.sh'` must stay green (no lint regression from the fix).
    - Prefer fixes that keep upgrade/restore/settings consistent (see 06-RESEARCH.md pitfalls).
  </action>
  <verify>
    <automated>bash -n scripts/install scripts/upgrade scripts/restore scripts/backup scripts/_common.sh && git diff --quiet tests.toml && ! grep -rqi 'exclude' tests.toml</automated>
    <manual>Each fix maps to a quoted triage finding. Run the local linter gate and confirm it is still green.</manual>
    <sampling_rate>run after this task commits, before the next VM cycle</sampling_rate>
  </verify>
  <done>All in-scope package-bug findings have corresponding fixes; scripts pass `bash -n`; `tests.toml` untouched; local linter green.</done>
</task>

<task type="auto">
  <name>task 2: Re-run the FULL suite and re-triage (repeat until zero in-scope failures)</name>
  <files>.planning/phases/06-fix-findings-to-zero-failures/cycles/, .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md</files>
  <action>
    Repeat the cycle from Plan 03 Task 1 + Task 2 with the fixes applied:

    1. Sync the package to the VM, run the FULL suite (no targeted single-test re-runs — locked), retrieve artifacts into `cycles/cycle-N/`.
    2. Triage into the four buckets; add a `## Cycle N` section to the triage log.
    3. If any in-scope test still FAILs, return to task 1 with the new findings.
    4. Stop when every in-scope test shows SUCCESS (`change_url` may remain FAIL — exempt).

    Retry/flake discipline (locked):
    - If a cycle aborts on a concrete external cause (apt/CDN/DNS timeout, SSH drop, NIC flap), it may be re-run with NO code change — max 2 re-runs total for that cause. A pass after a no-change re-run is a **flake** (diagnostic only). A persistent failure after retries is a **regression** to fix.
    - Quote the external cause from the log for any flake claim. No cause in the log → regression.
    - Environment/harness tweaks (DHCP pinning, watchdog) are permissible between runs but must be noted in the triage log.
    - Keep a clear git rev per cycle so the "no code change between retries" rule is auditable.

    Record convergence in the triage log: the first cycle where all in-scope verdicts are SUCCESS (this candidate clean run feeds Plan 05 — but Plan 05 performs its own final run).
  </action>
  <verify>
    <automated>grep -qE '^## Cycle [2-9]' .planning/phases/06-fix-findings-to-zero-failures/triage/triage-log.md && test -d .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-2 && grep -q 'Global working time' .planning/phases/06-fix-findings-to-zero-failures/cycles/cycle-2/Test_results.log</automated>
    <manual>Confirm the latest cycle's in-scope verdicts are all SUCCESS, or that the remaining FAIL is inside an ongoing fix iteration (then continue the loop).</manual>
    <sampling_rate>after each full-suite cycle</sampling_rate>
  </verify>
  <done>All in-scope tests reach SUCCESS; the triage log shows the converging cycle history and the flake-vs-regression evidence for any retry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: User verifies the fix loop converged to zero in-scope failures</name>
  <files></files>
  <action>
    Present the converged fix-loop triage history to the user. All automated work (fixes + full-suite cycles) is already done; this checkpoint confirms convergence before the final clean run. Use the <what-built> / <how-to-verify> content below as the prompt.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — no automated command; human confirmation is the gate"</automated>
    <manual>User confirms all in-scope tests are SUCCESS in the latest cycle, change_url is the only FAIL (exempt), no environment issue was miscounted as a package fix, and no code changed between a flake retry and its passing run.</manual>
    <sampling_rate>gate before the final clean run</sampling_rate>
  </verify>
  <what-built>Fix loop converged to zero in-scope failures</what-built>
  <how-to-verify>
    Review the triage log: confirm every in-scope test (`install.root`, `backup_restore`, `upgrade`, `upgrade.05e3d5b`, `package_linter`) is SUCCESS in the latest cycle, `change_url` is the only FAIL (exempt), and the run completed (Global summary).
    Confirm no environment/harness issue was miscounted as a package fix, and that no code change occurred between a flake retry and its passing run.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the final clean run and archive, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `git diff tests.toml` empty.
- Local linter gate green.
- Latest cycle: all in-scope verdicts SUCCESS.
- Triage log documents convergence + flake/regression evidence.
</verification>

<success_criteria>
- Zero in-scope FAIL/ERROR across the latest full-suite cycle.
- All fixes traceable to triaged evidence.
- No frozen-file or fake-green violations.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-findings-to-zero-failures/06-04-SUMMARY.md`
</output>
